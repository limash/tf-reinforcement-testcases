from setuptools import setup

setup(name='tf_reinforcement_testcases',
      version='0.0.1',
      install_requires=['numpy',
                        'tensorflow',
                        'ray',
                        'reverb']
      )
